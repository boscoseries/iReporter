language: "node_js"
node_js:
  - "10.15.0"
  deploy:
    provider: heroku
    api_key:
      secure
services:
  - postgresql
before_script:
  - psql -c 'CREATE DATABASE ireporter;' -U postgres
  - npm run create
script:
  - npm run test
after_success: npm run coverage